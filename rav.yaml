scripts:
  server: cd services/api && uv run python manage.py runserver
  web: pnpm --filter web dev
  migrations: cd services/api && uv run python manage.py makemigrations
  migrate: cd services/api && uv run python manage.py migrate
  build: pnpm -r build
  start: pnpm --filter web start
